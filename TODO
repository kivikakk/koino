* use errdefer, actually recover without loss
  * test failed allocations at various stages
* investigate where we might be able to add comptime to parameters
* look for some places where we use booleans and replace with two-enums
* fix all our orderedRemove(0) uses
* look for XXX/TODOs
* CMARK_CTYPE_CLASS compare with std.ascii; probably dupe
* strings.trim -> std.mem.trim*
